const initialState = {
  isShowed: true,
  isAllowed: true,
  productCollections: [],
}

const productCollections = (state=initialState, action) => {
  return state
}

export default productCollections